<form method="post" action="{{ path('book_reserve', {'id': book.id}) }}" onsubmit="return confirm('Are you sure you want to reserve this item?');">
    <button class="btn btn-success w-100 mt-2">Reserve</button>
</form>